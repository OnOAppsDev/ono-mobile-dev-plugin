# Fixture: prose-false-positives

Healthy. Every slash-separated phrase below once produced a false defect from a naive
path regex. This corpus pins the shape filter: if someone replaces it with a bare
regex over the corpus, this fixture starts failing. Expected: zero defects.
