---
description: A demo command that routes to a placeholder with no gate.
argument-hint: [feature]
---

Plan the feature.

1. Invoke the confirmed platform's architect — `rn-architect` / `react-architect` — to propose an approach.
