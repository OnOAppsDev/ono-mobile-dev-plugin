---
description: A demo command whose React route is gated.
argument-hint: [feature]
---

Plan the feature.

1. Apply the matching dev-planning skill — `rn-dev-planning` / `react-dev-planning` — via that platform's architect agent (`rn-architect` / `react-architect`).

   - **Readiness gate — React.** Before invoking, check the target skill and agent for a "not yet authored / structure-only placeholder" marker. If present, stop and report that the platform methodology is not yet authored.
